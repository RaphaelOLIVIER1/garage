{% extends "layout/base.html.twig" %}
{% block title %}
Annonce
{% endblock title %}


{% block body %}
    <h1>Annonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annonce.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ annonce.title }}</td>
            </tr>
            <tr>
                <th>Kilometrage</th>
                <td>{{ annonce.kilometrage }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ annonce.marque }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ annonce.prix }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ annonce.annee }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img style="max-width: 70%;" src="{{ vich_uploader_asset(annonce,'imageFile') }}" alt=""></td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('app_annonce_index') }}">back to list</a>

    <a class="btn btn-primary" href="{{ path('app_annonce_edit', {'id': annonce.id}) }}">edit</a>

    {{ include('annonce/_delete_form.html.twig') }}
{% endblock %}
