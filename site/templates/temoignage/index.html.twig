{% extends "layout/base.html.twig" %}
{% block title %}
Temoignage index
{% endblock title %}
{% block body %}
<div class="m-5">

    <h1>Temoignage index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>String</th>
                <th>Note</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for temoignage in temoignages %}
            <tr>
                <td>{{ temoignage.id }}</td>
                <td>{{ temoignage.string }}</td>
                <td>{{ temoignage.note }}</td>
                <td>
                    <a href="{{ path('app_temoignage_show', {'id': temoignage.id}) }}" class="btn btn-primary">show</a>
                    <a href="{{ path('app_temoignage_edit', {'id': temoignage.id}) }}" class="btn btn-success">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_temoignage_new') }}" class="btn btn-primary">Create new</a>
</div>
{% endblock %}
