{{ form_start(form) }}
    {{ form_widget(form) }}
    {% if is_granted('ROLE_EDITOR') %}
    <button class="btn btn-success my-3">{{ button_label|default('Save') }}</button>
    {% else %}
    <button class="btn btn-success my-3">{{ button_label|default('Save') }}</button>
    {% endif %}
{{ form_end(form) }}
