<form method="post" action="{{ path('app_horraire_delete', {'id': horraire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ horraire.id) }}">
    <button class="btn">Delete</button>
</form>
